import { Inter, Montserrat_Alternates } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

const titleFont = Montserrat_Alternates({ subsets: ["latin"], weight: ["500", "700"] });

export { inter, titleFont };
